setuptools~=68.2.0
numpy~=1.24.4
opencv-python~=4.12.0.88
seaborn~=0.13.2
scipy~=1.10.1

# ── ROS1 (installati tramite apt su Linux, non pip) ───────────────────────
# sudo apt install ros-noetic-rospy
# sudo apt install ros-noetic-cv-bridge
# sudo apt install ros-noetic-sensor-msgs
# sudo apt install ros-noetic-geometry-msgs
# sudo apt install ros-noetic-std-msgs
# Pacchetti pip per metadati ROS (funzionano su Windows/Linux):
rospkg
catkin_pkg
